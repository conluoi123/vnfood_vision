# ============================================================
# requirements-deploy.txt — dành riêng cho Deploy lên Render
# Loại bỏ các package nặng/không cần thiết khi chạy production
# ============================================================

# Core ML — CPU-only để tránh pull CUDA (~700MB thay vì ~2GB)
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu
numpy
pillow

# Vision (dùng headless để nhẹ hơn opencv-python đầy đủ)
opencv-python-headless

# HuggingFace — download model weights từ Hub
huggingface_hub

# RAG & Embedding
chromadb
sentence-transformers
rank-bm25
scikit-learn

# LLM / Gemini API
google-generativeai

# Text to Speech
gtts

# Config & Utils
pyyaml
python-dotenv
tqdm

# Google Drive download (dùng trong startup.sh để tải model checkpoints)
gdown

# Backend API
fastapi
uvicorn[standard]
pydantic
python-multipart

# HTTP
requests
beautifulsoup4

# Data
pandas

